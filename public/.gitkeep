# This directory exists to satisfy Vercel's requirement for an output directory.
# This project uses serverless functions only (no static files needed).
# The index.html file provides a simple landing page.
